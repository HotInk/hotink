page.replace_html 'attach_mediafiles_list', :partial => 'mediafile', :collection => @mediafiles
page << "$('attach_mediafiles_list').select('li').each( function (t) { a = new Card(t); if (a.checkbox.checked) a.select(); });"
