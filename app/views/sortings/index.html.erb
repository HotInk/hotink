<h1>Article categories</h1>

<h3><%= @account.name + " - " + @article.title %> </h3>

<%= render :partial => 'article_sortings' %>

<br />
<%= link_to 'Reload categories', :url=>account_article_sortings_path(@account, @article, :format=>:js), :method => :get%> |

