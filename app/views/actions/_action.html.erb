<div id="action_container">
	<h1>Action</h1>
	
	<% for content_type in @action.content_types %>
		<h3><%= content_type %></h3>
		<ul>
			<% for record in @records[content_type] %>
			<li><%= record.title %></li>
			<% end %>
		</ul>
	<% end %>
</div>